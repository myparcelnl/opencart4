{# JSON i18n payload + the delegated click handler for the order action buttons.
   The payload is pre-encoded with JSON_HEX_TAG so it cannot break out of the
   script element; it must not be |e'd. #}
<script id="myparcel-i18n" type="application/json">{{ i18n }}</script>
<script src="{{ src|e }}"></script>
